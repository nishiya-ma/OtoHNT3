#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

#define HEIGHT 1200
#define WIDTH 800

typedef struct {
	int x, y;
	int enemyNum;
	bool isDestroyed;
} enemy

int main(void) {
	unsigned int delay = 1;
	sleep(delay);
	return 0;
}

int init(void) {
		
}
